# Local publish script (contains personal paths)
publish.js

# macOS
.DS_Store

# Node
node_modules/

# Claude local settings
.claude/settings.local.json

# Projects — each subfolder is its own git repo, not tracked here
projects/
